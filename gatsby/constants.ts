import path from 'path'

export const CATEGORY_FOLDER_NAME = '__category__'

export const PATH_ROOT = path.resolve( __dirname, '../' )
export const PATH_CONTENT = path.resolve( PATH_ROOT, 'build-contents' )